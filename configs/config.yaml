server:
  port: "8099"
  mode: "debug"  # debug, release, test
  host: "0.0.0.0"

database:
  host: "localhost"
  port: "3306"
  database: "fftest"
  username: "root"
  password: "eisoo.com123"
  ssl: "false"

security:
  jwt_secret: "your-super-secret-jwt-key-change-in-production"
  jwt_expiration: "24h"
  rate_limit_per_minute: 60
  rate_limit_burst: 10
  enable_auth: false  # 临时禁用认证，让 JWT 先通过
  enable_rate_limit: true

logging:
  level: "debug"  # debug, info, warn, error
  format: "json"  # json, text

query:
  prefer_streaming: true
  execution_mode: "auto"  # auto, streaming, pagination

mcp:
  enabled: true  # Set to true to run as MCP server
  transport: "stdio"  # Options: stdio, http, sse
  port: "8090"  # Port for HTTP transport
  host: "0.0.0.0"  # Host for HTTP transport